#include<stdio.h>
/**
 * main - Entry point 
 *
 * Return:Always 0(Success)
 */
int main()
{
	int h;
	int k;

	for (h = 30; h <= 39; h++)
		putchar (h);
	for (k = 61; k <= 66; k++)
		putchar (k);
	putchar(10);
	return (0);
}
