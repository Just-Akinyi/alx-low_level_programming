#include <stdio.h>
/**
 *displays alphabets
 */
int main(void)
{
        char ch;

        for (c = 'a'; c <= 'z'; ++c)
		printf("%c", c);
	for (c = 'A'; c <= 'Z'; ++c)
                printf("%c", c);
        printf("\n");
        return (0);
}
