#include <stdio.h>
void main()
{
	int ch;
        /* print uppercase letters */

	for (ch= ‘a’; ch<= ‘z’; ch++)
             /* print lowercase letters */
	for (ch= ‘A’; ch<= ‘Z’; cputchar(ch);
			putchar("%c,"ch);
			putchar("\n");
	return(0);

}
